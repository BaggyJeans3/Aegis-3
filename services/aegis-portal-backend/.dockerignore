# 로컬 개발 산출물은 리포지토리에는 두되 운영 이미지에는 포함하지 않음.

venv/
.venv/

__pycache__/
*.pyc
*.pyo
*.pyd

.env
.env.local
*.log

# 운영 시 수동 제거 항목(README의 "운영 배포 시 정리 항목" 참고).
# seed_data.py 는 main.py 에서 import 하므로 .dockerignore 로 제외하면 빌드가 깨짐.
# 코드 편집으로 분리 후 같이 제거해야 함.
